# Update User first, then copy it to the correct location.
# sudo cp atuin-systemd /etc/systemd/system/atuin.service
[Unit]
Description=Docker Compose Atuin Service
Requires=docker.service
After=docker.service

[Service]
User=TO_BE_FILLED_IN
# Where the docker-compose file is located
WorkingDirectory=%h/dotfiles/atuin-server
ExecStart=/usr/bin/docker compose up -d
ExecStop=/usr/bin/docker compose down
TimeoutStartSec=0
Restart=on-failure
StartLimitBurst=3

#[Install]
WantedBy=multi-user.target
